# Local Expo dev config — not safe to commit
# Expo requires EXPO_PUBLIC_ prefix for vars accessible in app code at runtime
EXPO_PUBLIC_API_MODE=mock
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Set EXPO_PUBLIC_API_MODE=live and point at your running Voqstra backend to use the real API
