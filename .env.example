# Server
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=voqstra
DB_USER=voqstra
DB_PASSWORD=voqstra123

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Gemini API (free tier — get at https://aistudio.google.com)
GEMINI_API_KEY=AIza...your_key_here

# Nodemailer — Ethereal (auto-created at runtime, no config needed)
# Leave blank. A test account is auto-generated.
# After each send, a preview URL is logged to the console.

# Upload settings
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=25
